---
interface Props {
  tags: string[];
}

const {tags} = Astro.props;

---

<ul style="display:flex;gap:8px;list-style-type:none;">{tags.map((tag) => <li style="border:1px solid rgb(192, 192, 192);padding: 1px 4px;border-radius:4px;">{tag}</li>)}</ul>