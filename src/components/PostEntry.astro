---
interface Props {
  title: string;
  id: string;
  publishDate?: string;
}

import Link from '@/components/Link.astro';

const { title, id } = Astro.props;
---

<Link href={`/posts/${id}`}>
  {title}
</Link>
